import { TestBed } from '@angular/core/testing';

import { DebilidadesxpersonaService } from './debilidadesxpersona.service';

describe('DebilidadesxpersonaService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: DebilidadesxpersonaService = TestBed.get(DebilidadesxpersonaService);
    expect(service).toBeTruthy();
  });
});
