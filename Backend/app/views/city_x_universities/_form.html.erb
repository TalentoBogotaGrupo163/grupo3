<%= form_with(model: city_x_university, local: true) do |form| %>
  <% if city_x_university.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city_x_university.errors.count, "error") %> prohibited this city_x_university from being saved:</h2>

      <ul>
      <% city_x_university.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :universities_id %>
    <%= form.text_field :universities_id %>
  </div>

  <div class="field">
    <%= form.label :city_id %>
    <%= form.text_field :city_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
