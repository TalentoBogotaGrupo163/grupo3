<%= form_with(model: gender, local: true) do |form| %>
  <% if gender.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gender.errors.count, "error") %> prohibited this gender from being saved:</h2>

      <ul>
      <% gender.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code_gender %>
    <%= form.number_field :code_gender %>
  </div>

  <div class="field">
    <%= form.label :name_gender %>
    <%= form.text_field :name_gender %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
