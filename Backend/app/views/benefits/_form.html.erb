<%= form_with(model: benefit, local: true) do |form| %>
  <% if benefit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(benefit.errors.count, "error") %> prohibited this benefit from being saved:</h2>

      <ul>
      <% benefit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code_benefit %>
    <%= form.number_field :code_benefit %>
  </div>

  <div class="field">
    <%= form.label :name_benefit %>
    <%= form.text_field :name_benefit %>
  </div>

  <div class="field">
    <%= form.label :description_benefit %>
    <%= form.text_field :description_benefit %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
