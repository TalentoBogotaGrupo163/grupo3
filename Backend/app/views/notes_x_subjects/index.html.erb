<p id="notice"><%= notice %></p>

<h1>Notes X Subjects</h1>

<table>
  <thead>
    <tr>
      <th>Code note</th>
      <th>Code subject</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @notes_x_subjects.each do |notes_x_subject| %>
      <tr>
        <td><%= notes_x_subject.code_note %></td>
        <td><%= notes_x_subject.code_subject %></td>
        <td><%= notes_x_subject.description %></td>
        <td><%= link_to 'Show', notes_x_subject %></td>
        <td><%= link_to 'Edit', edit_notes_x_subject_path(notes_x_subject) %></td>
        <td><%= link_to 'Destroy', notes_x_subject, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Notes X Subject', new_notes_x_subject_path %>
