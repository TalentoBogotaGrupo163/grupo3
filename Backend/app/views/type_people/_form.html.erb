<%= form_with(model: type_person, local: true) do |form| %>
  <% if type_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_person.errors.count, "error") %> prohibited this type_person from being saved:</h2>

      <ul>
      <% type_person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_type %>
    <%= form.text_field :name_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
