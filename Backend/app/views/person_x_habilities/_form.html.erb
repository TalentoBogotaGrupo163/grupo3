<%= form_with(model: person_x_hability, local: true) do |form| %>
  <% if person_x_hability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(person_x_hability.errors.count, "error") %> prohibited this person_x_hability from being saved:</h2>

      <ul>
      <% person_x_hability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hability_id %>
    <%= form.text_field :hability_id %>
  </div>

  <div class="field">
    <%= form.label :person_id %>
    <%= form.text_field :person_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
