<%= form_with(model: note_x_subject, local: true) do |form| %>
  <% if note_x_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note_x_subject.errors.count, "error") %> prohibited this note_x_subject from being saved:</h2>

      <ul>
      <% note_x_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :note_id %>
    <%= form.text_field :note_id %>
  </div>

  <div class="field">
    <%= form.label :subject_id %>
    <%= form.text_field :subject_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
