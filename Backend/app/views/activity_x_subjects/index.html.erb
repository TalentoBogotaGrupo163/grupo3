<p id="notice"><%= notice %></p>

<h1>Activity X Subjects</h1>

<table>
  <thead>
    <tr>
      <th>Subject</th>
      <th>Activity</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @activity_x_subjects.each do |activity_x_subject| %>
      <tr>
        <td><%= activity_x_subject.subject %></td>
        <td><%= activity_x_subject.activity %></td>
        <td><%= activity_x_subject.description %></td>
        <td><%= link_to 'Show', activity_x_subject %></td>
        <td><%= link_to 'Edit', edit_activity_x_subject_path(activity_x_subject) %></td>
        <td><%= link_to 'Destroy', activity_x_subject, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Activity X Subject', new_activity_x_subject_path %>
