<%= form_with(model: activity_x_subject, local: true) do |form| %>
  <% if activity_x_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activity_x_subject.errors.count, "error") %> prohibited this activity_x_subject from being saved:</h2>

      <ul>
      <% activity_x_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :subject_id %>
    <%= form.text_field :subject_id %>
  </div>

  <div class="field">
    <%= form.label :activity_id %>
    <%= form.text_field :activity_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
