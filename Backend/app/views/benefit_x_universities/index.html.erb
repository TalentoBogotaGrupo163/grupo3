<p id="notice"><%= notice %></p>

<h1>Benefit X Universities</h1>

<table>
  <thead>
    <tr>
      <th>Code benefit</th>
      <th>Code university</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @benefit_x_universities.each do |benefit_x_university| %>
      <tr>
        <td><%= benefit_x_university.code_benefit %></td>
        <td><%= benefit_x_university.code_university %></td>
        <td><%= benefit_x_university.description %></td>
        <td><%= link_to 'Show', benefit_x_university %></td>
        <td><%= link_to 'Edit', edit_benefit_x_university_path(benefit_x_university) %></td>
        <td><%= link_to 'Destroy', benefit_x_university, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Benefit X University', new_benefit_x_university_path %>
