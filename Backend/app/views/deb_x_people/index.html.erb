<p id="notice"><%= notice %></p>

<h1>Deb X People</h1>

<table>
  <thead>
    <tr>
      <th>Code debility</th>
      <th>Code person</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @deb_x_people.each do |deb_x_person| %>
      <tr>
        <td><%= deb_x_person.code_debility %></td>
        <td><%= deb_x_person.code_person %></td>
        <td><%= deb_x_person.description %></td>
        <td><%= link_to 'Show', deb_x_person %></td>
        <td><%= link_to 'Edit', edit_deb_x_person_path(deb_x_person) %></td>
        <td><%= link_to 'Destroy', deb_x_person, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Deb X Person', new_deb_x_person_path %>
