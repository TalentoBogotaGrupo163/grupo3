<%= form_with(model: activities_x_subject, local: true) do |form| %>
  <% if activities_x_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activities_x_subject.errors.count, "error") %> prohibited this activities_x_subject from being saved:</h2>

      <ul>
      <% activities_x_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Subjects_id %>
    <%= form.text_field :Subjects_id %>
  </div>

  <div class="field">
    <%= form.label :Activities_id %>
    <%= form.text_field :Activities_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
