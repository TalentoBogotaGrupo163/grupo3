<%= form_with(model: type_user, local: true) do |form| %>
  <% if type_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_user.errors.count, "error") %> prohibited this type_user from being saved:</h2>

      <ul>
      <% type_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code_type %>
    <%= form.number_field :code_type %>
  </div>

  <div class="field">
    <%= form.label :name_type %>
    <%= form.text_field :name_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
